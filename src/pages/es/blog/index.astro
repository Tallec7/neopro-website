---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog', ({ id }) => id.startsWith('es/'))).sort(
  (a, b) => b.data.date.getTime() - a.data.date.getTime()
);

const title = 'Blog — Señalización deportiva, patrocinadores y digital';
const description = "Consejos prácticos para clubes deportivos: señalización deportiva digital, pantallas LED, valorización de patrocinadores y animación de partidos.";
---

<BaseLayout title={title} description={description} noAlternates locale="es">
  <!-- Hero -->
  <section class="bg-madxp-night px-[20px] md:px-[40px] lg:px-[60px] py-[60px] md:py-[96px]">
    <div class="max-w-[900px]">
      <p class="text-[16px] tracking-[2px] text-[#d9d9d9] uppercase mb-[20px]">BLOG</p>
      <h1 class="text-[36px] md:text-[50px] lg:text-[70px] leading-[1.1] text-white mb-[20px]">
        <span class="font-display">Recursos para</span><br />
        <span class="font-black">tu club</span>
      </h1>
      <p class="text-[18px] text-[rgba(255,255,255,0.7)] max-w-[600px]">
        Guías prácticas, casos de éxito y consejos para profesionalizar tu club deportivo — del nivel pro al regional.
      </p>
    </div>
  </section>

  <!-- Articles -->
  <section class="px-[20px] md:px-[40px] lg:px-[60px] py-[60px] md:py-[96px]">
    <div class="max-w-[900px] mx-auto">
      <div class="flex flex-col gap-[40px]">
        {posts.map((post) => (
          <article class="group">
            <a href={`/es/blog/${post.id.replace('es/', '')}/`} class="block">
              <div class="flex flex-col gap-[12px]">
                <div class="flex items-center gap-[12px] text-[14px] text-madxp-gray">
                  <time datetime={post.data.date.toISOString().slice(0, 10)}>
                    {post.data.date.toLocaleDateString('es-ES', { year: 'numeric', month: 'long', day: 'numeric' })}
                  </time>
                  {post.data.tags.length > 0 && (
                    <>
                      <span>·</span>
                      <span>{post.data.tags[0]}</span>
                    </>
                  )}
                </div>
                <h2 class="text-[24px] md:text-[28px] font-bold text-[#0a0b0d] group-hover:text-madxp-blue-deep transition-colors">
                  {post.data.title}
                </h2>
                <p class="text-[16px] text-madxp-gray leading-relaxed">
                  {post.data.description}
                </p>
                <span class="text-madxp-blue-deep font-medium text-[16px] group-hover:underline">
                  Leer el artículo →
                </span>
              </div>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
